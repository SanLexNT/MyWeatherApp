package com.example.myweatherapp.screens.city

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class ChangeCityViewModel(application: Application) : AndroidViewModel(application) {
}